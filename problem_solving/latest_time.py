def get_first_digit(digits: list[int]) -> int:
    """Returns a digit between zero and two for the first digit of time"""
    ...


def get_second_digit(digits: list[int], first_digit: int) -> int:
    """Returns a digit between zero and nine if first_digit is not two, otherwise between zero and three"""
    ...


def get_third_digit(digits: list[int]) -> int:
    """Returns a digit between zero and five from a list"""
    ...
